namespace Backend.Models.Enums
{
    public enum ExecutionStatus
    {
        SUCCESS,
        NOT_FOUND,
        VALIDATION_ERROR,
        UNAUTHORISED,
        INTERNAL_SERVER_ERROR,
        ALREADY_EXISTS
    }
}
